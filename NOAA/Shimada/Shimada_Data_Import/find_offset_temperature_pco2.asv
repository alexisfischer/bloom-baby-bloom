%% match up t45 temperature (Sci sw system) with pco2 intake temperature to assess offset

clear;
filepath= '~/Documents/MATLAB/bloom-baby-bloom/NOAA/Shimada/'; 

load([filepath 'Data/environ_Shimada2019'],'DT','LON','LAT','TEMP','TEMPi','PCO2');

%% matchup
isnan(TEMP)

%%
figure;
plot(DT,TEMP,'-',DT,TEMPi,'-');
legend('TSG 45','pCO2IntakeTemp')

